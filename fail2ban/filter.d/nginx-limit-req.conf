# Fail2ban filter for nginx limit_req
# Detects when a client IP exceeds Nginx's request rate limits

[Definition]
failregex = ^\s*\[error\] \d+#\d+: \*\d+ limiting requests, excess: [\d\.]+ by zone "[^"]+", client: <HOST>,
ignoreregex =
