# ── PurrfectPal Studio – Docker Compose Secrets ──────────────────────────────
# Copy this file to .env and fill in the real values.
# NEVER commit the .env file to git.

# GitHub webhook secret (must match the secret in your GitHub webhook settings)
WEBHOOK_SECRET=Suw@s@77m@g@r!

# GitHub Personal Access Token for git pull during deploy
# Needs: repo → read content  (fine-grained PAT is preferred)
GITHUB_PAT=

# Docker Hub credentials for pulling private images
DOCKER_USER=manojshrees
DOCKER_PASS=

# GitHub token for the DB backup service (push backups to backup repo)
GITHUB_TOKEN=
